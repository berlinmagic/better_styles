<div style="display: block; position: fixed; top: 0; left: 0; right: 0; background: rgba(0,0,0,.25); color: #fff; z-index: 1111; padding: 5px 50px;">
  
  <button class="btn btn-warning btn-sm pull-right">
    render pages
  </button>
  
  <% PagesController::TEMPLATES.each do |tmpl| %>
    <%= link_to tmpl.titleize, "/#{tmpl}", class: "btn btn-sm btn-link" %>
  <% end %>
  
</div>